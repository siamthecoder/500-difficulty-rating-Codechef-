#include <bits/stdc++.h>
using namespace std;

int main()
{
    int N, X, t;

    cin >> t;

    while (t != 0)
    {
        cin >> N >> X;

        cout << N - X;

        t--;
    }

    return 0;
