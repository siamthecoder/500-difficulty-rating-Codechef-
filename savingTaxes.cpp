#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x, y, t;

    cin >> t;
    while (t != 0)
    {
        cin >> x >> y;
        cout << x - y << endl;
        t--;
    }

    return 0;
}